# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# IDE
/.idea
.ruff_cache
.coverage
**/.coverage

# Test cache
.pytest_cache/

# Jupyter
.ipynb_checkpoints/

# Sim results
sim/results/

# Build outputs (regenerated by poe generate-model / generate-asm)
# output/ — tracked so GitLab shows deliverables

# Secrets
secrets.env
*.erc
*.log
*_sklib.py
skidl_REPL.*

# KiCad generated outputs (regenerated by poe build)
# *.kicad_sch — tracked so GitLab shows schematic
# *.net — tracked so GitLab shows netlist

# Schematic generation code (regenerated by /generate-schematic)
# cad/schematic/ — tracked so GitLab shows generated code

# KiCad temp files
*.kicad_prl
*-backups/
*.lck
fp-info-cache
*-erc.rpt
.claude/settings.local.json
